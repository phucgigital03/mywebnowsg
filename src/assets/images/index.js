const images = {
    logo: require('./logo.png'),
    tee: require('./tee.webp'),
    pant: require('./pant.webp'),
    sweater: require('./sweater.webp'),
};

console.log(images.tee);
console.log(images.sweater);
console.log(images.pant);

export default images;
